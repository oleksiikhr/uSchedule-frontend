const state = {
  darkTheme: false
}

const mutations = {
  CHANGE_DARK_THEME (state) {
    state.darkTheme = !state.darkTheme
  }
}

const actions = {

}

export default {
  state, mutations, actions
}
